# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
.venv*/
venv/
env/
*.egg
dist/
build/
*.whl

# Testing & linting
.pytest_cache/
.ruff_cache/
.mypy_cache/
.coverage
htmlcov/
.tox/
*.coverage
coverage.xml

# Node
node_modules/
frontend/dist/

# Docker
.env
.env.local
.env.*.local

# IDE
.vscode/
.idea/
*.swp
*.swo
*~
*.iml

# OS
.DS_Store
Thumbs.db

# Project-specific
*.db
*.log
.cache/
data/

# Vercel
.vercel/

# Astro
.astro/

# OpenCode / AI tooling
.atl/
.agents/
.mcp.json
skills-lock.json

# Oracle VM keys
.oci-vm-key
.oci-vm-key.pub

# Stale deployment files
railway.json
backend/fly.toml

# Pi harness config (gentle-ai) — local only
.pi/

# direnv — local only. El .envrc de este repo activa el env de conda `storico`,
# que es la eleccion del owner y no un mandato del repo (ver AGENTS.md). Si se
# versionara, cualquiera que clone con direnv se comeria un `layout_anaconda`
# que ademas crea el env si no existe.
# .direnv/ entra por las dudas: `layout python` crea un venv ahi adentro, y este
# repo ya tuvo un incidente de .venv accidental en el working tree.
.envrc
.direnv/
